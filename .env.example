# PostgreSQL (Docker Compose ou Render)
DATABASE_URL="postgresql://album:album@localhost:5432/album?schema=public"

# Clerk — Dashboard → API Keys → Secret key
CLERK_SECRET_KEY="sk_test_..."

# Origens do frontend (separadas por vírgula). Inclua localhost e a URL da Vercel em produção.
FRONTEND_ORIGINS="http://localhost:3000"

# Porta HTTP (Render injeta PORT em produção)
PORT=4000
HOST=0.0.0.0
